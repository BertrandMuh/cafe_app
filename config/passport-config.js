
// passport (and other libraries use "strategies")

const localStrategy = require('passport-local').Strategy


module.exports = async function (passport) {
    //1. use the local strategy (if email exists and passport match)

    //2 . 
}